"""Baaki as a product: multi-tenant persistence, auth, billing and the operator UI.

The simulation package (`baaki.*`) is unchanged and still runs standalone — this layer reuses
its policy gate, toolbox and brains verbatim against real merchant data.
"""
